export const WHEEL_SIZE = 260;
export const WHEEL_RADIUS = WHEEL_SIZE / 2;
export const CENTER_RADIUS = 56; // CenterButton radius (112 / 2)

export const RING_BUTTON_SIZE = 44;
export const RING_BUTTON_OFFSET = (WHEEL_SIZE - RING_BUTTON_SIZE) / 2;
export const RING_BUTTON_INSET = 12;

export const DEGREES_PER_BPM = 10;
export const RADIANS_PER_BPM = (DEGREES_PER_BPM * Math.PI) / 180;
